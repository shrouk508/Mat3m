@model Mat3m.Models.Category

@{
    ViewData["Title"] = "Details";
}

<h1>تفاصيل</h1>

<div>
    <h4>القسم</h4>
    <hr />
    <div class="container-fluid">
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Name)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-info">Edit</a> |
        <a asp-action="Index" class="btn btn-dark" style="margin:5px">Back to List</a>
    </div>
</div>